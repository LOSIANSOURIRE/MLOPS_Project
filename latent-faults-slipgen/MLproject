name: latent-faults-slipgen
conda_env: conda.yaml
entry_points:
  main:
    parameters:
      learning_rate: {type: float, default: 0.001}
      batch_size: {type: int, default: 32}
    command: "python scripts/train_mapper_decoder.py --learning_rate {learning_rate} --batch_size {batch_size}"