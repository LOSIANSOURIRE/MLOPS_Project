route:
  receiver: 'webhook_receiver'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'webhook_receiver'
    webhook_configs:
      - url: 'http://backend:8000/api/alerts' # Placeholder URL
